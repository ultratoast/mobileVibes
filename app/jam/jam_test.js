'use strict';

describe('mobileVibes.jam module', function() {

  beforeEach(module('mobileVibes.jam'));

  describe('jam controller', function(){

    it('should ....', inject(function() {
      //spec body
      var jamCtrl = this('JamCtrl');
      expect(jamCtrl).toBeDefined();
    }));

  });
});