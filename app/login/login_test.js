'use strict';

describe('mobileVibes.login module', function() {

  beforeEach(module('mobileVibes.login'));

  describe('login controller', function(){

    it('should ....', inject(function() {
      //spec body
      var loginCtrl = this('LoginCtrl');
      expect(loginCtrl).toBeDefined();
    }));

  });
});