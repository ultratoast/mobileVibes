'use strict';

describe('mobileVibes.browse module', function() {

  beforeEach(module('mobileVibes.browse'));

  describe('browse controller', function(){

    it('should ....', inject(function( {
      //spec body
      var browseCtrl = this('BrowseCtrl');
      expect(browseCtrl).toBeDefined();
    }));

  });
});